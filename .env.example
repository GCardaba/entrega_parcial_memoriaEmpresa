# PostgreSQL (Homebrew local install — brew services start postgresql@16)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=optimizer_db
DB_USER=optimizer_admin
DB_PASSWORD=practicas

# LLM API — only one needed; comment out the other
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Application
LOG_LEVEL=INFO
